# Database
DATABASE_URL=mysql+aiomysql://root:xcqlife0918@localhost:3306/wechat_finetune

# Redis (Not strictly needed if CELERY_ALWAYS_EAGER=True)
REDIS_URL=redis://localhost:6379/0

# Storage
# Set to True to use local filesystem instead of MinIO
USE_LOCAL_STORAGE=True
LOCAL_STORAGE_PATH=./storage_data

# MinIO (Ignored if USE_LOCAL_STORAGE=True)
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_UPLOADS=uploads
MINIO_BUCKET_MODELS=models
MINIO_SECURE=False

# Celery
# Set to True to run tasks synchronously (no Redis required)
CELERY_ALWAYS_EAGER=True

# Security
SECRET_KEY=dev-secret-key-only-for-testing
ACCESS_TOKEN_EXPIRE_MINUTES=30

# App
PROJECT_NAME=WeChatFinetune

OLLAMA_BASE_MODEL=llama3.1:8b
